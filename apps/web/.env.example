# apps/web/.env.example
# Copy to .env for local development

PUBLIC_API_URL=http://localhost:8787
PUBLIC_APP_NAME=StoreBuilder
PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Auth note: sessions are HttpOnly cookies — no token storage in .env needed.
# The web app uses credentials:'include' on all fetch calls to API.

# For production (Cloudflare Pages env vars):
# PUBLIC_API_URL=https://api.yourdomain.com
# PUBLIC_APP_NAME=YourBrandName
# PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...
